image: Ubuntu

stack: jdk 11

branches:
  only:
    - master

build: off

environment:
  nodejs_version: "12"

install:
  - java -jar ./artifacts/aqa-shop.jar &
  - chmod +x gradlew
  - npm install
  - npm start ./artifacts/gate-simulator/

build_script:
  - ./gradlew test -Dselenide.headless=true --info
